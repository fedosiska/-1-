MODULE 1 (Create, addElement, removeElement, print, delete Vector):
    int a = 1;
    int b = 2;
    int c = 3;
    Vector* v1 = createVector(INT);
    addElement(v1, &a, INT);
    //будет ошибка
    addElement(v1, &b, FLOAT);
    //addElement(v1, &b, INT);
    addElement(v1, &c, INT);
    printVector(v1);
    //будет ошибка
    removeElement(v1,5);
    //removeElement(v1,0);
    printVector(v1);
    //будет ошибка
    printElement(v1,5);
    //printElement(v1,1);
    deleteVector(v1);

MODULE 2 (multiplyVector, addVectors, distVector):
    int a = 1;
    int b = 2;
    double c = 3;
    double d = 4;
    //будет ошибка
    double e = 2;
    //int e = 2;
    Vector* v1 = createVector(INT);
    Vector* v2 = createVector(DOUBLE);
    addElement(v1,&a,INT);
    addElement(v1,&b,INT);
    addElement(v2,&c,DOUBLE);
    addElement(v2,&d,DOUBLE);
    //будет ошибка
    Vector* v3 = multiplyVector(v1,&e, DOUBLE);
    //Vector* v3 = multiplyVector(v1,&e, INT);
    printVector(v3);
    distVector(v3);
    //будет ошибка
    Vector* v4 = addVectors(v1,v2);
    //Vector* v4 = addVectors(v1,v3);
    printVector(v4);
    removeElement(v1,1);
    //будет ошибка
    Vector* v5 = addVectors(v4,v1);
    printVector(v5);

MODULE 3 (scalarProduct, vectorProduct):
    int x = -3;
    int y = 2;
    int c = 5;
    int d = 4;
    int e = 10;
    int f = 9;
    int g = -2;
    Vector* v1 = createVector(INT);
    //будет ошибка
    Vector* v2 = createVector(DOUBLE);
    scalarProduct(v1, v2);
    //Vector* v2 = createVector(INT);
    addElement(v1,&x, INT);
    addElement(v1,&y, INT);
    addElement(v1,&c, INT);
    addElement(v2,&d, INT);
    addElement(v2,&e, INT);
    //будет ошибка
    scalarProduct(v1, v2);
    //addElement(v2,&f, INT);
    //scalarProduct(v1,v2);
    //будет ошибка
    addElement(v2, &g, INT);
    //removeElement(v2, 3);
    Vector* v3 = vectorProduct(v1,v2);
    printVector(v3);
    distVector(v3);
    